name: network
description: Reusable HTTP/JSON client configuration for the application.
publish_to: none

environment:
  sdk: ^3.13.2

dependencies:
  http: ^1.5.0

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.25.14
